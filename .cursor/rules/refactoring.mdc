---
alwaysApply: true
---

Правила для Cursor AI при выполнении рефакторинга проекта
Версия: 1.0
Применимо для: Задача рефакторинга нейро-техподдержки
Дата: 14.02.2026

КРИТИЧНЫЕ ПРАВИЛА РЕФАКТОРИНГА
🔴 Правило #1: Безопасность превыше всего
НИКОГДА не удаляй код, если не уверен на 100%, что он не используется.
Перед удалением:

Поиск всех использований в проекте
Проверка тестов
Поиск в логах упоминаний функции
Комментирование на несколько дней вместо удаления

python# ❌ НЕПРАВИЛЬНО - сразу удалять
# def some_function():
#     pass

# ✅ ПРАВИЛЬНО - пометить и проверить через время
# @deprecated("Will be removed in v2.0. Use new_function() instead")
# def some_function():
#     """DEPRECATED: Use new_function() instead"""
#     logger.warning("Deprecated function called")
#     return new_function()

1. Приоритеты оптимизации
1.1. Порядок действий (строго соблюдать!)
1. АУДИТ → Понять, что есть
2. ТЕСТЫ → Убедиться, что все работает
3. БЭКАП → Сохранить текущее состояние
4. ИЗМЕНЕНИЯ → Применить рефакторинг
5. ТЕСТИРОВАНИЕ → Проверить работоспособность
6. ИЗМЕРЕНИЯ → Подтвердить улучшения
7. ДОКУМЕНТАЦИЯ → Обновить README и комментарии
ЗАПРЕЩЕНО: Начинать с удаления кода или зависимостей без аудита!
1.2. Метрики успеха (проверять после каждого изменения)
bash# После каждого изменения запускать:

# 1. Размер образа
docker images neuro-support:latest --format "{{.Size}}"

# 2. Все тесты
pytest tests/ -v

# 3. Линтинг
flake8 app/
mypy app/

# 4. Неиспользуемые импорты
autoflake --check --recursive app/

# Если хоть одна проверка провалилась → откатить изменения!

2. Удаление зависимостей
2.1. Процесс удаления библиотеки
bash# ШАГ 1: Найти все использования
grep -r "import library_name" app/
grep -r "from library_name" app/

# ШАГ 2: Проверить в requirements.txt размер
pip show library_name | grep Size

# ШАГ 3: Закомментировать в requirements.txt
# library_name==1.0.0  # TESTING REMOVAL - size: 500MB

# ШАГ 4: Пересобрать контейнер
docker-compose build --no-cache

# ШАГ 5: Запустить тесты
pytest tests/

# ШАГ 6: Проверить функциональность
# - Telegram бот отвечает?
# - Админка открывается?
# - RAG поиск работает?

# ШАГ 7: Если все ОК → удалить из requirements.txt
# Если НЕТ → откатить изменения!
2.2. Список для проверки при удалении
Sentence-transformers (приоритет #1):
python# НАЙТИ ВСЕ ИСПОЛЬЗОВАНИЯ:
grep -r "SentenceTransformer" app/
grep -r "sentence_transformers" app/

# ПРОВЕРИТЬ, ЧТО ЗАМЕНИЛИ НА:
# from openai import AsyncOpenAI
# embedding = await openai_client.embeddings.create(...)

# ПОСЛЕ ЗАМЕНЫ:
# 1. Пересоздать FAISS индексы
# 2. Проверить качество поиска (RAG)
# 3. Сравнить результаты со старой версией
Torch / TensorFlow:
bash# Должны быть удалены ПОЛНОСТЬЮ
grep -r "torch" app/  # Должно вернуть 0 результатов
grep -r "tensorflow" app/  # Должно вернуть 0 результатов
Pandas:
python# ОСТАВИТЬ только для экспорта в админке
# Использовать lazy import:

# ❌ БЫЛО
import pandas as pd

# ✅ СТАЛО
def export_to_csv(data: List[Dict]) -> str:
    """Pandas импортируется только при экспорте"""
    import pandas as pd  # Lazy import
    df = pd.DataFrame(data)
    return df.to_csv(index=False)

3. Оптимизация Docker
3.1. Multi-stage build (обязательно!)
dockerfile# СТРУКТУРА (нарушать нельзя!):
# Stage 1: Builder - только для компиляции
FROM python:3.11-slim as builder
# Установить build-зависимости
# Собрать wheels
# Очистить кэш

# Stage 2: Runtime - финальный образ
FROM python:3.11-slim
# Копировать только готовые wheels
# Установить runtime зависимости
# Копировать приложение
# Очистить всё лишнее
3.2. Правила оптимизации слоев
dockerfile# ✅ ПРАВИЛЬНО - группировать команды
RUN apt-get update && \
    apt-get install -y --no-install-recommends gcc g++ && \
    rm -rf /var/lib/apt/lists/*

# ❌ НЕПРАВИЛЬНО - каждая команда = новый слой
RUN apt-get update
RUN apt-get install -y gcc
RUN apt-get install -y g++
RUN rm -rf /var/lib/apt/lists/*

# ✅ ПРАВИЛЬНО - копировать только нужное
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY ./app /app/app

# ❌ НЕПРАВИЛЬНО - копировать всё
COPY . /app/
RUN pip install -r requirements.txt
3.3. .dockerignore (обязательно создать!)
# КРИТИЧНО: игнорировать всё, что не нужно в контейнере

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
venv/
env/

# Tests
tests/
*.test.py
pytest.ini
.coverage

# Documentation
docs/
*.md
!README.md

# IDE
.vscode/
.idea/
.git/

# Logs и данные (монтируются отдельно)
logs/
*.log
data/*.db
data/faiss_index/

# CI/CD
.github/
.gitlab-ci.yml
docker-compose.yml

4. Рефакторинг RAG Engine
4.1. Замена sentence-transformers на OpenAI
КРИТИЧЕСКИ ВАЖНО: Не удалять sentence-transformers до полной замены!
python# ПЛАН МИГРАЦИИ:

# ШАГ 1: Добавить новый метод с OpenAI (НЕ удалять старый!)
class RAGEngine:
    async def create_embedding_openai(self, text: str) -> List[float]:
        """Новый метод через OpenAI"""
        response = await self.openai_client.embeddings.create(
            model="text-embedding-3-small",
            input=text
        )
        return response.data[0].embedding
    
    # Старый метод пока оставить
    def create_embedding_local(self, text: str) -> List[float]:
        """DEPRECATED: старый метод через sentence-transformers"""
        return self.model.encode(text)

# ШАГ 2: Переключить использование на новый метод
# ШАГ 3: Пересоздать FAISS индексы
# ШАГ 4: Протестировать качество поиска
# ШАГ 5: Если OK → удалить старый метод и зависимость
4.2. Тестирование качества RAG
python# ОБЯЗАТЕЛЬНО протестировать ПЕРЕД удалением sentence-transformers

TEST_QUERIES = [
    "Как записаться на программу?",
    "Стоимость обучения",
    "Что такое прямая передача способностей?",
    "Работа с Ангелами",
    "Формат обучения"
]

async def test_rag_quality():
    """Сравнение качества old vs new embeddings"""
    
    for query in TEST_QUERIES:
        # Старый метод
        old_results = await rag_old.search(query, top_k=3)
        
        # Новый метод (OpenAI)
        new_results = await rag_new.search(query, top_k=3)
        
        print(f"Query: {query}")
        print(f"Old scores: {[r['score'] for r in old_results]}")
        print(f"New scores: {[r['score'] for r in new_results]}")
        print(f"Old chunks: {[r['text'][:100] for r in old_results]}")
        print(f"New chunks: {[r['text'][:100] for r in new_results]}")
        print("-" * 80)
        
        # КРИТЕРИЙ: новый метод должен быть НЕ ХУЖЕ старого
        assert new_results[0]['score'] >= old_results[0]['score'] * 0.9

5. Работа с кодом
5.1. Удаление неиспользуемых импортов
bash# АВТОМАТИЧЕСКИ (но проверить результат!)
autoflake --remove-all-unused-imports --in-place --recursive app/

# ВРУЧНУЮ проверить критичные файлы:
# - app/main.py
# - app/core/rag_engine.py
# - app/core/ai_client.py
5.2. Поиск мёртвого кода
bash# Найти неиспользуемые функции
vulture app/ --min-confidence 80 > dead_code_report.txt

# ВНИМАНИЕ: НЕ удалять автоматически!
# Проверить каждую функцию вручную:
# 1. Может использоваться динамически
# 2. Может быть API endpoint
# 3. Может быть для будущего использования
5.3. Lazy imports для тяжёлых библиотек
python# ✅ ПРАВИЛЬНО - pandas только при экспорте
async def export_questions_csv() -> bytes:
    """Pandas загружается только при вызове"""
    import pandas as pd  # Lazy import
    
    questions = await get_all_questions()
    df = pd.DataFrame(questions)
    return df.to_csv(index=False).encode()

# ❌ НЕПРАВИЛЬНО - pandas всегда в памяти
import pandas as pd  # В начале файла

async def export_questions_csv() -> bytes:
    questions = await get_all_questions()
    df = pd.DataFrame(questions)
    return df.to_csv(index=False).encode()

6. Тестирование после рефакторинга
6.1. Обязательные тесты перед коммитом
bash#!/bin/bash
# pre-commit-checks.sh

echo "🔍 Running pre-commit checks..."

# 1. Линтинг
echo "📝 Linting..."
flake8 app/ --max-line-length=100 || exit 1

# 2. Type checking
echo "🔢 Type checking..."
mypy app/ || exit 1

# 3. Неиспользуемые импорты
echo "🗑️ Checking unused imports..."
autoflake --check --recursive app/ || exit 1

# 4. Unit тесты
echo "🧪 Running tests..."
pytest tests/ -v || exit 1

# 5. Размер образа
echo "📦 Building Docker image..."
docker build -t neuro-support:test . || exit 1

SIZE=$(docker images neuro-support:test --format "{{.Size}}")
echo "📏 Image size: $SIZE"

# Проверка лимита 7 GB (в байтах примерно 7.5e9)
SIZE_BYTES=$(docker images neuro-support:test --format "{{.Size}}" | numfmt --from=iec)
if [ "$SIZE_BYTES" -gt 7500000000 ]; then
    echo "❌ Image too large! Must be ≤ 7 GB"
    exit 1
fi

echo "✅ All checks passed!"
6.2. Функциональное тестирование
python# Чеклист функциональности (проверять ВРУЧНУЮ):

# ✅ Telegram Bot
# 1. Отправить /start
# 2. Задать вопрос "Как записаться?"
# 3. Задать вопрос "Стоимость"
# 4. Задать вопрос не по теме
# 5. Отправить 21 запрос подряд (проверка rate limit)

# ✅ Админка
# 1. Войти в /admin
# 2. Открыть базу знаний
# 3. Изменить текст и сохранить
# 4. Пересоздать индексы
# 5. Открыть аналитику
# 6. Экспортировать вопросы в CSV

# ✅ API
# curl http://localhost:8000/health
# Должен вернуть: {"status": "healthy", ...}

7. Измерение результатов
7.1. Метрики BEFORE / AFTER
bash# Создать файл metrics.txt

# === BEFORE REFACTORING ===
BEFORE_IMAGE_SIZE=$(docker images neuro-support:before --format "{{.Size}}")
BEFORE_BUILD_TIME=600  # секунды
BEFORE_RAM_IDLE=1024   # MB

echo "BEFORE:"
echo "  Image size: $BEFORE_IMAGE_SIZE"
echo "  Build time: $BEFORE_BUILD_TIME sec"
echo "  RAM idle: $BEFORE_RAM_IDLE MB"

# === AFTER REFACTORING ===
docker build -t neuro-support:after -f Dockerfile.optimized .

AFTER_IMAGE_SIZE=$(docker images neuro-support:after --format "{{.Size}}")
AFTER_BUILD_TIME=180  # измерить фактическое время
AFTER_RAM_IDLE=$(docker stats neuro-support --no-stream --format "{{.MemUsage}}")

echo "AFTER:"
echo "  Image size: $AFTER_IMAGE_SIZE"
echo "  Build time: $AFTER_BUILD_TIME sec"
echo "  RAM idle: $AFTER_RAM_IDLE MB"

# === IMPROVEMENT ===
echo "IMPROVEMENT:"
echo "  Image size: -$(echo "scale=1; ($BEFORE_IMAGE_SIZE - $AFTER_IMAGE_SIZE) / $BEFORE_IMAGE_SIZE * 100" | bc)%"

8. Документирование изменений
8.1. CHANGELOG.md (обязательно обновить!)
markdown# Changelog

## [2.0.0-refactored] - 2026-02-14

### Changed
- Заменил sentence-transformers на OpenAI embeddings API
- Оптимизировал Docker образ с multi-stage build
- Уменьшил размер контейнера с ~15 GB до ~6 GB

### Removed
- Удалил неиспользуемые зависимости:
  - sentence-transformers (~2.5 GB)
  - torch (~1.5 GB)
  - aiohttp (дублировал httpx)
  
### Added
- Lazy imports для pandas
- Автоматическая очистка старых вопросов
- Health check в docker-compose

### Performance
- Время сборки: 10 мин → 4 мин
- Время старта: 20 сек → 8 сек
- RAM idle: 1 GB → 512 MB
8.2. README.md (СОЗДАТЬ ЧЕРЕЗ CURSOR!)
КРИТИЧЕСКИ ВАЖНО: README.md должен быть создан Cursor AI автоматически после завершения всех этапов рефакторинга!
Промпт для Cursor:
На основе выполненного рефакторинга создай полностью новый README.md 
для проекта нейро-техподдержки Школы Михаила Агеева.

КОНТЕКСТ РЕФАКТОРИНГА:
- Уменьшили Docker образ с 15 GB до 6 GB
- Заменили sentence-transformers на OpenAI Embeddings API
- Удалили torch, tensorflow, aiohttp
- Реализовали multi-stage Docker build
- Добавили .dockerignore
- Оптимизировали requirements.txt

ТРЕБОВАНИЯ К README:
1. Включи раздел "Что нового в v2.0" с метриками
2. Обнови архитектуру (убери sentence-transformers)
3. Обнови установку с новыми инструкциями
4. Добавь Changelog с v2.0.0-refactored
5. Обнови Troubleshooting
6. Используй профессиональный стиль с emoji

См. детальные требования в ТЗ (раздел 8).
Структура README (обязательно):

Заголовок с badges (version, size, python)
Краткое описание
⭐ Что нового в v2.0 (with metrics!)
Быстрый старт
Требования
Установка
Конфигурация
Использование
Архитектура (без sentence-transformers!)
Разработка
Мониторинг
Troubleshooting
Обновление
FAQ
⭐ Changelog (с v2.0.0-refactored)
Лицензия и контакты

Проверка перед сохранением:

 Нет упоминаний sentence-transformers
 Нет упоминаний torch/tensorflow
 Метрики актуальны (6 GB, 4 мин, 512 MB)
 Все примеры кода рабочие
 Changelog обновлен

8.3. Комментарии в коде
python# При ИЗМЕНЕНИИ кода добавлять комментарий:

# REFACTORING 2026-02-14: Заменил sentence-transformers на OpenAI embeddings
# Причина: Уменьшение размера Docker образа на 2.5 GB
# Старый код сохранён в git: commit abc123
async def create_embedding(self, text: str) -> List[float]:
    """Создание embedding через OpenAI API (было: sentence-transformers)"""
    response = await self.openai_client.embeddings.create(
        model="text-embedding-3-small",
        input=text
    )
    return response.data[0].embedding

9. Откат изменений
9.1. Git workflow
bash# ПЕРЕД началом рефакторинга:
git checkout -b refactoring/optimize-docker
git tag before-refactoring

# ПОСЛЕ каждого значительного изменения:
git add .
git commit -m "refactor: remove sentence-transformers, add OpenAI embeddings"

# В случае проблем:
git revert HEAD  # Откатить последний коммит
# ИЛИ
git reset --hard before-refactoring  # Откатить всё

# ПОСЛЕ успешного тестирования:
git checkout main
git merge refactoring/optimize-docker
git tag v2.0.0-refactored
git push origin main --tags
9.2. Docker rollback
bash# ПЕРЕД рефакторингом - создать backup образ:
docker tag neuro-support:latest neuro-support:backup-v1
docker save neuro-support:backup-v1 > neuro-support-backup.tar

# В случае проблем:
docker load < neuro-support-backup.tar
docker tag neuro-support:backup-v1 neuro-support:latest
docker-compose down && docker-compose up -d

10. Критерии готовности
✅ Готово к деплою, если:

 Размер Docker образа ≤ 7 GB
 Все тесты проходят (pytest coverage ≥ 80%)
 Линтинг без ошибок (flake8, mypy)
 Бот отвечает корректно на 10 тестовых вопросов
 Админка работает (все страницы открываются)
 Health check возвращает "healthy"
 Потребление RAM ≤ 600 MB в idle
 Время сборки ≤ 5 минут
 Время старта ≤ 10 секунд
 CHANGELOG.md обновлён
 README.md создан Cursor AI (см. раздел 8.2)
 Создан backup текущей версии
 Подготовлен план отката

❌ НЕ готово к деплою, если:

 Хоть один тест не проходит
 Размер образа > 7 GB
 Бот не отвечает или выдаёт ошибки
 Потребление RAM > 1 GB в idle
 Есть критичные warning в логах
 Нет backup текущей версии
 Не обновлена документация


11. Важные напоминания
🚨 НИКОГДА:

НЕ удалять код без тестов - сначала напиши тест, потом удаляй
НЕ коммитить без проверки - всегда запускай pre-commit checks
НЕ деплоить без backup - всегда создавай точку отката
НЕ удалять зависимости пачками - по одной с проверкой
НЕ менять логику без понимания - изучи код, потом меняй

✅ ВСЕГДА:

Читай код перед изменением - понимание важнее скорости
Тестируй после каждого изменения - маленькие итерации
Документируй причину изменений - комментарии и CHANGELOG
Измеряй результат - before/after метрики
Проси ревью - два взгляда лучше одного
Создай README через Cursor - после завершения рефакторинга (см. раздел 8.2)


12. Контрольный чек-лист
Перед финальным коммитом пройтись по списку:
bash# 1. Код
□ Все импорты используются
□ Нет мёртвого кода
□ Нет дублирования
□ Есть docstrings
□ Есть type hints

# 2. Тесты
□ Все тесты проходят
□ Coverage ≥ 80%
□ Нет skip тестов без причины

# 3. Docker
□ Размер ≤ 7 GB
□ .dockerignore создан
□ Multi-stage build
□ Очистка кэша

# 4. Зависимости
□ requirements.txt минимальный
□ Нет дублирующих библиотек
□ Все зависимости используются

# 5. Документация
□ README.md создан Cursor AI
□ CHANGELOG.md обновлён
□ Комментарии в коде
□ API документация

# 6. Безопасность
□ Нет хардкод секретов
□ .env в .gitignore
□ Нет SQL injection
□ Input validation

# 7. Производительность
□ RAM idle ≤ 512 MB
□ Время старта ≤ 10 сек
□ Время сборки ≤ 5 мин

# 8. Backup
□ Создан backup образ
□ Создан git tag
□ Подготовлен план отката
