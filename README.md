# Нейро-техподдержка Школы Михаила Агеева

Интеллектуальная система автоматической технической поддержки на базе GPT-4.1-mini с использованием технологии RAG (Retrieval-Augmented Generation) для консультирования учеников и посетителей Школы Михаила Агеева.

## 🌟 Основные возможности

- **Умные ответы**: Генерация ответов на основе базы знаний школы (Markdown-файл).
- **Мультиканальность**: Поддержка Telegram и Jivo Chat.
- **Память диалога**: Удержание контекста последних 5 сообщений для связного общения.
- **Аналитика**: Автоматическое выявление и сохранение уникальных вопросов пользователей.
- **Админ-панель**: Веб-интерфейс для управления базой знаний, просмотра статистики и тестирования бота.
- **Безопасность**: Встроенный Rate Limiting (20 запросов в час) и защита от спама.

## 🏗 Структура проекта

```text
neuro-support/
├── app/
│   ├── admin/              # Веб-интерфейс администратора (FastAPI + Jinja2)
│   ├── core/               # Ядро системы (RAG Engine, AI Client, Context)
│   ├── database/           # Работа с SQLite и аналитикой вопросов
│   ├── integrations/       # Обработчики Telegram и Jivo
│   ├── utils/              # Логирование и вспомогательные функции
│   ├── config.py           # Конфигурация через Pydantic Settings
│   └── main.py             # Точка входа FastAPI приложения
├── data/
│   ├── knowledge_base.md   # Исходный файл базы знаний (Markdown)
│   └── faiss_index/        # Векторная база данных FAISS
├── tests/                  # Тесты (pytest)
├── Dockerfile              # Инструкции для сборки образа
├── docker-compose.yml      # Описание сервисов (App + Nginx)
├── nginx.conf              # Конфигурация прокси-сервера
└── requirements.txt        # Зависимости Python
```

## 🚀 Быстрый старт

### 1. Подготовка окружения
Создайте файл `.env` в корне проекта (используйте `.env.example` как шаблон):
```bash
cp .env.example .env
```
Заполните обязательные поля:
- `OPENAI_API_KEY`: Ваш ключ OpenAI.
- `TELEGRAM_BOT_TOKEN`: Токен от @BotFather.
- `ADMIN_PASSWORD_HASH`: Пароль для входа в админку.

### 2. Запуск через Docker (рекомендуется)
```bash
docker-compose up -d --build
```

### 3. Инициализация базы знаний
После запуска необходимо векторизовать базу знаний:
1. Перейдите в админку: `http://localhost:8000/admin/knowledge`
2. Нажмите кнопку **"Пересоздать индексы"**.
3. Дождитесь уведомления об успешном завершении.

## 🛠 Порядок пользования

### Работа с Telegram
- Найдите вашего бота в Telegram и нажмите `/start`.
- Для очистки контекста диалога используйте команду `/clear_history`.

### Панель администратора
- **Аналитика**: Просмотр списка уникальных вопросов, их частоты и источников.
- **База знаний**: Прямое редактирование файла `knowledge_base.md`. После внесения правок обязательно нажимайте "Пересоздать индексы".
- **Тестирование**: Проверка ответов бота без необходимости писать в Telegram. Показывает, какие именно фрагменты базы знаний были использованы для ответа.

## ⚙️ Технологический стек

- **Backend**: Python 3.11, FastAPI
- **AI**: OpenAI SDK (GPT-4.1-mini, text-embedding-3-small)
- **Vector DB**: FAISS (Facebook AI Similarity Search)
- **Database**: SQLite + SQLAlchemy (Async)
- **Bot SDK**: Aiogram 3.x
- **Frontend**: Jinja2, Bootstrap 5

## 📝 Обновление базы знаний
Система использует семантический поиск. Чтобы бот отвечал лучше:
1. Используйте четкие заголовки (`##`, `###`) в Markdown.
2. Группируйте связанные данные в одном разделе.
3. Если бот дает неверный ответ, проверьте наличие этой информации в `knowledge_base.md` и обновите индексы.

---
*Разработано специально для Школы Михаила Агеева (2026)*
